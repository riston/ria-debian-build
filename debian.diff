Index: cmake/modules/FindPCSCLite.cmake
===================================================================
--- cmake/modules/FindPCSCLite.cmake	(revision 60)
+++ cmake/modules/FindPCSCLite.cmake	(working copy)
@@ -21,12 +21,14 @@
   ${PC_PCSCLITE_INCLUDEDIR}
   ${PC_PCSCLITE_INCLUDE_DIRS}
   ${PC_PCSCLITE_INCLUDE_DIRS}/PCSC
+  /usr/include/PCSC
   )
 
 FIND_LIBRARY(PCSCLITE_LIBRARY NAMES pcsclite libpcsclite PCSC
   HINTS
   ${PC_PCSCLITE_LIBDIR}
   ${PC_PCSCLITE_LIBRARY_DIRS}
+  /usr/lib/x86_64-linux-gnu
   )
 
 # handle the QUIETLY and REQUIRED arguments and set PCSCLITE_FOUND to TRUE if 
Index: packaging/ubuntu/debian_libdigidoc/control
===================================================================
--- packaging/ubuntu/debian_libdigidoc/control	(revision 60)
+++ packaging/ubuntu/debian_libdigidoc/control	(working copy)
@@ -2,7 +2,7 @@
 Section: libs
 Priority: extra
 Maintainer: ria <info@ria.ee>
-Build-Depends: cdbs, cmake, opensc, libssl-dev, libxml2-dev
+Build-Depends: cdbs, cmake, libssl-dev, libxml2-dev
 Standards-Version: 3.8.3
 Homepage: http://www.ria.ee
 
Index: packaging/build.sh
===================================================================
--- packaging/build.sh	(revision 60)
+++ packaging/build.sh	(working copy)
@@ -1,8 +1,5 @@
 #!/bin/bash
 
-ifconfig || echo "ifconfig not found"
-env
-
 if [ `uname` == "Darwin" ]
 then
   TARGET_OS=mac
@@ -34,7 +31,8 @@
   else
     TARGET_OS=ubuntu
   fi
-
+  # override to always build a DEB
+  TARGET_OS=ubuntu
   cd idkaart/current/packaging/$TARGET_OS
 
   SVN_REVISION=`svnversion . | sed -r "s/([0-9]*:)?([0-9]+).*?/\2/"`
@@ -48,12 +46,12 @@
   fi
   echo "======= module repository: $REPOSITORY"
 
-  REPOSITORY=/mnt/build-repository
+  REPOSITORY=${HOME}/repository
 
-  umount $REPOSITORY || echo "not mounted"
-  echo "mounting sshfs..."
-  sshfs administrator@10.0.25.57:$EXTERNAL_REPOSITORY $REPOSITORY -o nonempty
-  MOUNT_RESULT=$?
+#  umount $REPOSITORY || echo "not mounted"
+#  echo "mounting sshfs..."
+#  sshfs administrator@10.0.25.57:$EXTERNAL_REPOSITORY $REPOSITORY -o nonempty
+  MOUNT_RESULT=0
   if [ $MOUNT_RESULT -eq 0 ] ; then
     echo "remote $EXTERNAL_REPOSITORY mounted to $REPOSITORY"
    else
Index: qesteidutil/CMakeLists.txt
===================================================================
--- qesteidutil/CMakeLists.txt	(revision 60)
+++ qesteidutil/CMakeLists.txt	(working copy)
@@ -8,7 +8,7 @@
 include( GNUInstallDirs )
 include( VersionInfo )
 
-find_package(Qt4 4.7.0 COMPONENTS QtCore QtGui QtNetwork REQUIRED)
+find_package(Qt4 4.6.3 COMPONENTS QtCore QtGui QtNetwork REQUIRED)
 find_package(OpenSSL REQUIRED)
 find_package(PKCS11)
 find_package(SmartCardpp REQUIRED)
